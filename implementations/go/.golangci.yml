# golangci-lint configuration for the BlockParty SDK.
# Generated protobuf code under blockpb/ is excluded.
version: "2"

linters:
  default: standard
  exclusions:
    generated: lax
    paths:
      - blockpb
