{
  "name": "@blockparty/cli",
  "version": "0.1.0",
  "description": "Reference node/CLI for the BlockParty Protocol — a thin consumer of @blockparty/sdk.",
  "license": "CC0-1.0",
  "private": true,
  "type": "module",
  "bin": {
    "bp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@blockparty/sdk": "workspace:*"
  }
}
